## {{IMP}} · {{TITLE}}

Opened automatically by `/improve-auto` at `{{TIMESTAMP}}`. Auto-merge is enabled — this PR squash-merges into `main` when CI passes.

If this looks wrong, comment `/loop:halt` on the PR (or `/cancel-ralph` in the parent session) and the loop will skip future picks of this IMP.

---

### Implementer report

{{REPORT}}

---

### Verification gates passed before push

- `pnpm -w build` — green
- `pnpm -r --filter='!@humanchrome/wasm-simd' --filter='!humanchrome-monorepo' test` — green
- `node smoke-test.mjs` — green (bridge + extension)
- `pnpm e2e:isolated` — green (only when the diff touches `app/chrome-extension/`)
- `.claude/scripts/loop-guardrails.mjs` — ok

🤖 Generated by `/improve-auto` (autonomous loop)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
